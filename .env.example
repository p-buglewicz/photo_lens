# Environment variables for development
# Database
# For local development (outside Docker):
# DATABASE_URL=postgresql+psycopg://lensanalytics:lensanalytics_dev@localhost:5432/lensanalytics
# For Docker Compose services (inside the compose network):
DATABASE_URL=postgresql+asyncpg://lensanalytics:lensanalytics_dev@postgres:5432/lensanalytics

# Environment
ENVIRONMENT=development

# Ingestion
TAKEOUT_PATH=/mnt/photos/Takeout
THUMBNAIL_CACHE=/mnt/photos/thumbnails

# ML Models (optional - downloads from HuggingFace if not set)
# MODEL_PATH=/models
